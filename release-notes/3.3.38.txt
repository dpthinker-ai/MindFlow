3.3.38

- 回看聊天收紧了“主题简短总结”路径，像“我记了哪些人生建议，简单总结几句”这类问题不再误落到分析模板里
- 结构化回答解析顺序改成 JSON -> 标签 section -> 普通 section，减少 `依据 / 下一步 / 类别` 在 fallback 里被误拆或漏出的情况
- `类别` section 的 item 协议统一成“类别名称：说明”，并兼容清理旧的 `类别：...` 前缀，避免分类列表里重复出现“类别：”

MindFlow 3.3.1

- Flow 不再只围着最近最热的单个项目转，wiki 生成、维护器选材和 Flow 压缩上下文都扩展到了更多主题线，并显式避免在同一条线内部打转。
- 图谱页恢复记录热力图，并保留按年份切换和按天查看记录强度。
- 图谱节点范围从少量中心点扩展到完整网络视图，方向、结论、概念、问题、方法、实验和证据会一起进入画布。
- 旧的窄快照会在后台自动重刷，升级后不需要手动清缓存才能看到更完整的知识层结果。

# MindFlow Agents

Use `PROJECT_GUIDE.md` as the single source of truth for project guidance, release validation rules, emulator data/model preservation, recovery anchors, and session handoff notes.

Critical reminder: user-visible emulator validation must use the release APK at `app/build/outputs/apk/release/app-release.apk`. Do not install a debug APK, uninstall the app, clear app data, wipe the AVD, or delete local model/data files unless the user explicitly authorizes destructive reset.
